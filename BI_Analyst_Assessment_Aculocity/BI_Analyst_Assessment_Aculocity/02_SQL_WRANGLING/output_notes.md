/* ============================================================
   FINAL OUTPUT
   View: dbo.vw_customer_summary
   ------------------------------------------------------------
   One row per unique customer (business + address) with all
   required fields for segmentation and prioritization.

   Consumed by Power BI for visualization and analysis.
   ============================================================ */









